# 시연 전 확인 목록

**코드 프리즈: 2026-08-21 (금) 10:00.** 그 이후 레포에 푸시하지 않는다.

이 문서가 프리즈를 전제로 쓰였다. **마감 뒤에는 코드로 아무것도 못 고친다** —
그래서 아래는 "무엇을 미리 확인할 것인가"와 "푸시 없이 되는 복구는 무엇인가"로 나뉜다.

---

## 0. 프리즈 전에 끝낼 것 (~8/21 10:00)

- [x] ~~배포 브랜치 확정~~ — `main` 에 fast-forward 머지 완료 (2026-08-18). Railway watch 브랜치도 `main` 으로 변경됨
- [ ] 마지막 커밋이 Railway 에 실제로 배포됐는지 확인 (Deployments 최상단이 그 커밋인지)
- [ ] 기동 로그 3줄 확인 (아래 2번)
- [ ] 백엔드에서 실제 메시지 → 우리 로그에 판정 줄이 찍히는지 (아래 3번)

**프리즈 뒤에 남는 손잡이는 두 개뿐이다.**

| 수단 | 되는 것 |
| --- | --- |
| Railway **환경변수** 변경 + 재배포 | 쿨다운·기억 저장·마감 시간 조절 (5번) |
| Railway **이전 배포로 롤백** | 푸시가 아니라 배포 히스토리에서 고르는 것이라 프리즈에 안 걸린다 |

---

## 1. 외부 API — 시연 당일 아침에

- [ ] **OpenAI 잔액** — 개발 전체에 $4 썼다. 시연 규모면 $1 미만이지만 잔액이 0이면 워커가 통째로 죽는다
- [ ] **YouTube 쿼터** — 하루 10,000 units, 추천 1건에 약 106 units → **하루 약 94회**
- [ ] **카카오 키** — 데이트 코스가 이걸로 실재 장소를 가져온다

> ⚠️ **YouTube 쿼터 리셋은 태평양 자정 = KST 16:00 이다.** 오전에 리허설로 태우면
> 오후 4시까지 안 돌아온다. **시연이 오전이면 전날 오후 4시 이후로는 유튜브를 아껴야 한다.**

소진되면 **오류가 아니라 조용한 미발동**이 된다. 화면에서 "적절한 영상이 없었다"와
구분되지 않는다 — 이게 시연 중 가장 찾기 어려운 실패다.

---

## 2. 배포 상태 — Railway

- [ ] **Serverless(App Sleeping) 꺼져 있는지.** 켜져 있으면 첫 요청이 컨테이너 부팅을 기다린다
- [ ] 서비스 카드가 초록 `Active`
- [ ] 배포 로그에 이 세 줄:

```
INFO:     PORT=8000 · 외부 API 키 — openai=True kakao=True youtube=True
INFO:     Application startup complete.
INFO:     ::ffff:100.64.0.2:xxxxx - "GET /health HTTP/1.1" 200 OK
```

**키 하나라도 `False` 면 그 기능만 조용히 안 뜬다.** 워커는 죽지 않아서 배포 상태로는
절대 안 드러난다. 이 줄이 유일한 흔적이다.

---

## 3. 끝에서 끝까지 연결 — 실제 메시지 1건

백엔드에서 메시지를 보내면 우리 로그에 이 줄이 찍힌다.

```
INFO:     분석 <uuid> · N.N초 · COMPLETED · 결과=… 상태표현=2건
```

- [ ] `상태표현=2건` — 감정 분석이 나갔다 (위젯 ①번 줄)
- [ ] `결과=…` — 개입이 나갔다. **`결과=없음` 은 평범한 대화면 정상이다**
- [ ] 그 `<uuid>` 로 백엔드 로그·DB 를 맞대서 `emotion_analyses` 에 저장됐는지

**`200 OK` 만으로는 성공을 알 수 없다** — 오류도 `FAILED` 봉투에 담아 200으로 나간다.
반드시 위 판정 줄을 본다.

---

## 4. 시연 시나리오

`CLAUDE.md` 의 추천 순서. **`case7` → `case8` 을 붙여서 보여주는 게 가장 설득력 있다** —
똑같이 거친 표현인데 하나는 잡고 하나는 안 잡는다. 감지가 어휘 필터가 아니라 맥락 판정임을
한 번에 보여준다.

| 순서 | 케이스 | 보여주는 것 |
| --- | --- | --- |
| 1 | `case9_date` | RAG + 실재 장소 — 기억을 근거로 진짜 가게와 링크 |
| 2 | `case7_tone` | 갈등 중재 — 보낸 사람에게만 대체 문장 |
| 3 | `case8_banter` | **안전장치** — 같은 비속 표현인데 장난이면 아무것도 안 뜬다 |
| 4 | `case10_concern` | 유튜브 — 댓글까지 읽고 고른다 |

- [ ] 실제 앱으로 시연한다면 **각 케이스의 대화 대본을 미리 타이핑 연습**해 둔다
- [ ] 위젯 ①번 줄이 사람마다 다르게 보이는 것(A 화면 / B 화면)을 보여줄 준비
- [ ] 배포가 죽었을 때의 폴백 — CLI 와 devui 가 **같은 진입점**을 부른다

```bash
.venv/bin/python -m tools.run fixtures/case7_tone.json --verbose --no-persist
.venv/bin/python devui/server.py        # 브라우저에서 단계별로 보인다
```

> ⚠️ **데이트 코스를 보여주려면 대화에 지역명이 있어야 한다.** "성수", "연남동", "홍대"
> 같은 말이 대화나 기억에 없으면 **미발동한다** (2026-08-18 수정). 지역을 모르면 카카오가
> 전국 1위를 물어와서 커플과 상관없는 곳이 코스로 나가기 때문이다 — 실측에서 서울 중구,
> 충남 천안이 나왔다. `case9_date` · `case15_planning` 대본에는 지역명이 들어 있다.
>
> **"주말에 뭐 할까 / 영화나 볼까" 처럼 지역이 없는 대본으로는 코스가 안 뜬다.**
>
> 단, **'데이트' 낱말이 대본에 있으면** 강제 트리거(5장)가 잡아서 지역이 없어도
> `KAKAPO_DEMO_REGION`(성수)으로 뜬다. 위 경고는 낱말 없이 자연 발동시키는 대본에만 적용된다.

**평온한 상태(`STABLE`)를 보여주려면 대본을 지켜야 한다.** 감정 4축이 전부 3 미만이어야
`STABLE` 이다. "우와 신난다" 같은 말이 섞이면 `ENGAGED` 가 나온다. `case2_no_question`
대본(밋밋한 일상 보고)이 그 용도다.

---

## 5. 시연 직전 환경변수 — 푸시 없이 조절되는 것

Railway Variables 에서 바꾸고 재배포하면 적용된다. **프리즈 뒤에 남는 거의 유일한 손잡이다.**

| 변수 | 기본 | 상태 (2026-08-19 기준) |
| --- | --- | --- |
| `KAKAPO_DEMO_TRIGGERS` | 꺼짐 | **✅ `1` 로 이미 켜둠** — 방금 발화에 '데이트'/'유튜브' 낱말이 보이면 게이트·억제·LLM 보류·**최근 추천 중복 제외**를 건너뛰고 해당 추천을 무조건 태운다. "말했는데 안 뜨는" 사고 방지. 금지어 필터·실장소/실영상 원칙·자수 한도는 그대로 |
| `KAKAPO_DEMO_REGION` | 성수 | 위 모드에서 대화에 지역이 없을 때의 기본 지역. 시연 대본이 지역을 말하면 안 쓰인다 |
| `KAKAPO_YOUTUBE_COOLDOWN_MIN` | 5 | 그대로 — 키워드 트리거는 어차피 쿨다운을 무시한다. **키워드 없이** 자연 발동을 반복 리허설할 때만 `0` |
| `KAKAPO_PERSIST` | 1 | **✅ `0` 으로 이미 꺼둠** — 시드 27건 보존, 반복 재현성. 아래 참조 |
| `KAKAPO_DEADLINE` | 25 | 그대로 둔다 (백엔드 30초보다 앞에서 끊는 값) |

**`KAKAPO_DEMO_TRIGGERS` 는 마지막 발화만 본다.** 백엔드가 메시지 1건마다 워커를 부르므로,
키워드가 든 그 메시지에서 한 번 뜨고 다음 메시지부터는 평소 판정으로 돌아간다.
'유튜브' 낱말 1건마다 검색 쿼터 ~105 units(하루 95회분)가 나간다 — 리허설에서 수십 번만 안 보내면 된다.

배포 환경 검증 (2026-08-19, 켠 상태):
"심심한데 유튜브나 볼까" → 영상 12초 / "이번 주말에 데이트 가고 싶다" → 성수 폴백 코스 13초 /
**연속 트리거 2연발**("성수에서 데이트할까?" → 3초 뒤 "데이트 코스 추천해줘") → 12.2초·9.5초 둘 다 발동,
장소 구성은 자연스럽게 달라짐. 연속 발동은 처음엔 실패했는데(중복 제외가 방금 준 장소를 다 빼서
조용히 미발동) 데모 모드가 중복 제외를 건너뛰도록 고쳤다 (fb6bbf2).

> ⚠️ **고민 갈래(냉각기 영상) 연출을 하려면 대본에 '유튜브' 낱말을 넣지 않는다.**
> 낱말이 보이면 강제 트리거가 **화제 갈래**를 태워서, 안내 문구가 고민용이 아니라
> 화제용으로 나간다. 고민 갈래는 낱말 없이 서먹한 대화만으로 자연 발동시킨다 (`case10` 대본).

**대본 팁 — 키워드 옆에 소재·지역을 같이 말한다.** "유튜브에서 캠핑 영상 볼까"처럼
소재가 있으면 영상 질이 좋아지고("유튜브나 볼까"만 하면 아무 영상이 잡힌다),
"성수에서 데이트할까"처럼 지역이 있으면 폴백 없이 대화 그대로 코스가 나온다.
발동 자체는 소재가 없어도 보장된다.

**`KAKAPO_PERSIST=0` 이 반복 시연의 핵심이다.** 켜져 있으면 소환된 기억에 `used_at` 이
찍혀서 **두 번째 시연부터 데이트 코스 결과가 달라진다.** 30일 중복 방지가 걸리기 때문이다.

> 기억이 이미 오염됐으면 **재배포하면 초기화된다.** `data/memories.json` 은 컨테이너 로컬이라
> 시드 27건으로 돌아간다. 푸시가 필요 없는 복구 수단이다.

---

## 6. 조용히 실패하는 것들 — 증상과 확인 위치

시연 중 "왜 안 뜨지"가 나오는 지점을 미리 모아둔다. **전부 오류가 아니라 미발동으로 보인다.**

| 증상 | 원인 후보 | 어디서 보나 |
| --- | --- | --- |
| 유튜브가 안 뜬다 | 쿼터 소진 / 같은 화제에 이미 냄 / 백스톱 / 말투 교정이 발동함 | 워커 로그의 `보류=` — 억제·말투 보류는 이유가 남는다. **쿼터 소진만 흔적이 없다** |
| 데이트 코스가 안 뜬다 | **지역명 없음** / 카카오 키 / 자리 3개(밥·구경·카페)를 못 채움 | 기동 로그의 `kakao=`, 그리고 `--verbose`. 지역 문제면 `지역 단서 없음` 이 이유로 남는다 |
| ①번 줄이 안 뜬다 | 백엔드가 `emotion_analyses` 저장을 못 함 | 우리 로그엔 `상태표현=2건` 인데 화면에 없으면 백엔드 쪽 |
| 위젯이 옛날 상태로 되돌아감 | **응답 순서 역전** | 아래 참조 |
| 아무 반응이 없다 | 요청이 안 닿음 | 워커 로그에 `POST` 줄 자체가 없다 → 주소·포트·환경변수 |

**응답 순서 역전** — 메시지 1건마다 워커를 부르는데 요청마다 4~11초가 걸린다. 대화를 빠르게
치면 먼저 보낸 요청이 나중에 끝날 수 있고, 그러면 **오래된 감정 상태가 최신 것을 덮는다.**
워커가 막을 수 없다(요청 하나만 보고 상태를 안 갖는다). **백엔드가 저장 시점에
막아야 한다.** 시연에서 대화를 천천히 치면 안 나온다.

---

## 7. 백엔드에 프리즈 전까지 받아야 할 답

- [ ] **감정 라벨 4종**(`RESOLVED` · `ACCUMULATED` · `ENGAGED` · `ESCALATED`)도 저장되는지.
      회귀 테스트가 실제로 통과시킨 값은 `STABLE` 하나뿐이다. Enum 에 값이 없으면
      역직렬화 단계에서 **같은 방식으로 조용히** 떨어진다
- [ ] **응답 순서 역전 방어**가 있는지 (위 6번)
- [ ] `stateText` 를 프론트까지 전달하는지 — 저장은 확인됐다

---

## 확인 완료된 것 (참고)

프리즈 전까지 다시 볼 필요 없는 항목이다.

| | 확인일 |
| --- | --- |
| HTTP 서버 · 사설망 연결 (`ai-worker.railway.internal:8000`) | 2026-08-18 |
| 백엔드 → 워커 실제 요청 도달 (`POST … 200 OK`) | 2026-08-18 |
| `emotionType` · `intensityValue 0.0` · `results: []` 저장 통과 | 2026-08-18 |
| `stateText` 저장됨 | 2026-08-18 |
| 요청 빈도 = 메시지 1건마다 (A 방식) | 2026-08-18 |
| 동시 요청 3건이 서로 안 밀림 (전체 8.0초) | 2026-08-18 |

# kakapo AI 워커

해커톤 프로젝트. 커플의 채팅을 실시간 분석해 대화 소재를 제안하는 **kakapo**의 AI 워커 파트.

## 이 레포의 범위

**이 레포는 AI 워커만 담당한다.** 채팅 서버(Spring Boot)는 팀원이 별도 레포에서 개발 중이다.

- ✅ 이 레포: 대화 분석 → 개입 판정 → 소재 산출 (위젯 페이로드 생성)
- ❌ 이 레포 아님: 채팅 API, 실시간 전송, 프론트엔드, 인증, 배포 인프라

## 팀

| 역할 | 담당 |
| --- | --- |
| PM | 영환 |
| 디자인 | 혜원 |
| 프론트엔드 | 민상 |
| 채팅 서버 (Spring Boot) | 팀원 |
| AI 워커 (이 레포) | 나 |

---

## 서비스 개요

카카오톡 같은 채팅방 **1개**. 커플 두 명(A, B)이 대화한다.

```
┌─────────────────────────────┐
│  kakapo 위젯 (상단 고정)     │  ← AI 출력은 전부 여기
├─────────────────────────────┤
│  A: 오늘 뭐했어?             │
│  B: 그냥 집에 있었어          │  ← 커플만의 공간
│  A: 아 그렇구나              │     봇 메시지 삽입 금지
├─────────────────────────────┤
│  [입력창]                    │
└─────────────────────────────┘
```

**핵심: 위젯은 사람마다 다르게 보인다.** 같은 방이지만 상단만 개인화된다.

| | A 화면 | B 화면 |
| --- | --- | --- |
| 공통 코멘트 | 보임 | 보임 (동일 내용) |
| 개별 코멘트 (대상 A) | 보임 | **안 보임** |
| 대화 스트림 | 동일 | 동일 |

B에게 개별 코멘트가 갔을 때 A는 그런 게 떴다는 사실 자체를 모른다. 이게 의도된 설계다 — 지적성 피드백이 상대에게 노출되면 그 자체가 갈등 소재가 된다.

**위젯 슬롯은 1개다.** 공통과 개별 중 하나만 뜬다. 새 코멘트가 오면 이전 것을 교체한다.

---

## 절대 제약 (이 프로젝트의 핵심 가치)

**관계 상태를 언급하는 문구를 절대 출력하지 않는다.**

금지 예시:
- "요즘 대화가 줄었어요"
- "권태기 같아요"
- "두 분 사이가 서먹해 보여요"
- "대화가 뜸해지셨네요"

이유: 누군가에게 "너네 권태기다"라는 말을 들으면 의식이 심해지고, 평소 하지 않던 생각까지 하게 되어 오히려 관계가 악화된다.

**내가 눈치채기 전에 kakapo가 먼저 눈치채고 조용히 회복시킨다** — 이게 서비스의 핵심 가치다.

따라서 **감지 로직과 발화 로직은 완전히 분리된다.** 트리거는 갈등·권태를 감지하지만, 출력은 그것을 절대 드러내지 않는다. 출력은 그냥 자연스러운 대화 소재 하나일 뿐이다. 이 분리가 이 프로젝트 설계 난이도의 대부분을 차지한다.

프롬프트를 수정하거나 새 출력 문구를 만들 때 이 제약을 반드시 확인할 것.

### 이 제약이 걸리는 범위 — 헷갈리기 쉬움

금지 대상은 **관계 상태를 규정하는 말**이다. 감정 상태를 언급하는 것 전부가 아니다.

| | 예시 | |
| --- | --- | --- |
| ❌ 관계 규정 | "요즘 대화가 줄었어요", "권태기", "사이가 서먹" | 모든 후보 기능에서 금지 |
| ✅ 감정 알아봄 | "지금 감정이 조금 올라와 있는 것 같아요" | **갈등 중재에서는 오히려 이게 기능이다** |

대화 소재는 "관찰당했다"는 티조차 내면 안 된다. 반면 갈등 중재는 지금 감정이 올라온 걸
짚어주는 게 목적이고, 본인도 이미 아는 상태이며, 그 화면은 상대에게 보이지 않는다.

다만 갈등 중재에서도 **사람 자체를 평가하지는 않는다.** "무례하시네요"는 안 되고,
표현을 두고 "공격적으로 들릴 수 있어요"는 된다. 대상이 사람이면 안 되고 말이면 된다.
이건 하드 필터가 아니라 프롬프트에서 다룬다 — 단어 목록으로는 두 경우를 구분할 수 없다.

---

## 아키텍처

### MVP — 구현 완료

**위젯 슬롯은 1개다.** 후보 기능이 여러 개 발동해도 하나만 나간다.
`router.py`가 우선순위 순으로 시도하고, 먼저 결과를 내는 후보가 이긴다.

```
fixtures/case.json
   ↓
router.route()
   │
   ├─ 후보 1. 갈등 중재 (tone)  ← 갈등이 감지된 순간에 소재를 던질 때가 아니다
   │     check_tone_gate()   룰 — 공격·오해 유발 표현 6종, 개인 기준선 대비
   │        ↓ 걸리면
   │     tone_judge()        LLM — 진짜 갈등인가, 맥락상 장난인가
   │        ↓ 진짜면
   │     tone_suggest()      LLM — 방향 문구 + 대체 문장 (나 전달법)
   │        ↓ 장난이면 아래 후보로 내려간다
   │
   └─ 후보 2. 대화 소재 (topic)
         check_gate()        룰 게이트 (트리거 ①②③⑤)
            ↓ needs_llm 이면
         judge()             LLM 판정 (트리거 ④, 바쁨 판별) + 기억 추출
            ↓
         retrieve()          RAG — 대화 맥락과 유사한 기억 검색
            ↓
         make_topic()        소재 생성 (기억 기반) 또는 오늘의 질문
   ↓
filter()      금지어 하드 필터
   ↓
Decision      콘솔 출력
```

### 후속 — 서비스 연결

```
[채팅 서버 / Spring Boot] ──LPUSH ai:jobs──▶ [Redis]
        ▲                                      │
        │                                    BRPOP
        │                                      ▼
        │                              [이 레포: AI 워커]
        │                                      │
   POST /internal/widget ◀────────────────────┘
                                               │
[Postgres] ◀── read + memories write ──────────┘
```

워커는 서버가 아니다. 포트를 열지 않는 **무한 루프 프로세스**가 된다.

> ⚠️ 회신 API 스펙(`/internal/widget`)은 **채팅 서버 담당자와 재합의 필요**. 기존 `docs/contract-v2.md`는 방 구조가 바뀌면서 폐기됐다. MVP 단계에서는 구현하지 않는다.

---

## 기술 스택

- **Python 3.11+**
- **LangChain 1.x** — `langchain`, `langchain-openai`
- **RAG** — `OpenAIEmbeddings` + `InMemoryVectorStore` (기억 검색)
- **numpy** — `InMemoryVectorStore`의 cosine similarity 계산에 필요하다. 없으면 RAG가 `ImportError`로 죽는다

LangChain을 import하는 파일은 `worker/llm.py`와 `worker/retrieve.py` 둘뿐이다.
나머지는 전부 순수 파이썬이다. LangChain은 OpenAI를 부르는 어댑터로만 쓰고,
판정 품질을 만드는 로직(게이트·필터·라우터)은 직접 짠 코드다.

### MVP 범위 (현재 단계)

**지금 만드는 것은 판정 엔진이다.** 채팅 데이터를 넣으면 적절한 개입을 산출하는 함수.

| | MVP | 후속 |
| --- | --- | --- |
| 입력 | `fixtures/*.json` | Redis 큐 |
| 대화/기억 저장 | `data/memories.json` | Postgres |
| 출력 | 콘솔 | 채팅 서버 POST |
| 배선 | 함수 호출 + `router.py` | LangGraph `StateGraph` |

Redis·Postgres·회신 API·LangGraph는 **판정 품질에 기여하지 않는다.** 후속으로 미룬다.
지금 단계에서 이것들을 구현하지 않는다. 필요해 보여도 사용자에게 먼저 확인할 것.

### LangChain 규칙 (중요)

이 프로젝트는 LangChain 1.x를 쓴다. 0.x 시절 API는 전부 폐기됐다.

- ❌ `langchain-classic` 설치 금지
- ❌ `LLMChain`, `initialize_agent`, `AgentType`, `ConversationBufferMemory`, `LLMRouterChain` 등 레거시 API 금지
- ❌ `create_agent` 미사용 — judge는 툴 루프가 없는 단발 분류 호출이다. 에이전트를 끼우면 레이턴시와 비용만 늘어난다
- ❌ `import openai` 직접 호출 금지. 모든 LLM 호출은 LangChain 레이어를 거친다 (트레이싱 일관성)
- ✅ `init_chat_model` + `with_structured_output(Schema, method="json_schema", strict=True)`
- ⏸️ `langgraph`는 MVP에서 쓰지 않는다. 후보가 2개라 `router.py`의 우선순위 체인으로 충분하다. 후보가 더 늘어 우선순위만으로 못 정하게 되면 그때 도입
- ✅ 폴더명은 `nodes/` (`chains/` 아님 — chain은 레거시 어휘)

인터넷에서 찾은 LangChain 예제 대부분은 0.x 기준이다. 참고하기 전에 위 금지 목록에 걸리는지 확인할 것.

---

## 폴더 구조

```
AI_worker/
├── CLAUDE.md                    ← 이 문서 (프로젝트 상시 맥락)
├── README.md                    ← 코드 설명서
├── docs/
│   └── worker-tasks.md          ← 작업 지시서
├── .env.example
├── requirements.txt
├── fixtures/                    ← 케이스별 입력 대화 (case1~case8)
├── data/
│   ├── memories.json            ← 기억 시드 27건
│   ├── daily_questions.json     ← 오늘의 질문 30개
│   └── speaker_profiles.json    ← 개인 말투 기준선 시드 (A·B)
├── tools/
│   └── run.py                   ← CLI 러너
└── worker/
    ├── models.py                ← Pydantic 스키마
    ├── llm.py                   ← LLM 접근 레이어 (모든 호출이 여기를 거친다)
    ├── router.py                ← 후보 기능 중재 (우선순위 체인)
    ├── filter.py                ← 금지어 하드 필터
    ├── pipeline.py              ← 조립 (Context 만들어 라우터에 넘김)
    │
    ├── gate.py                  ← [소재] 룰 게이트 (트리거 ①②③⑤)
    ├── judge.py                 ← [소재] LLM 판정 + 기억 추출
    ├── retrieve.py              ← [소재] RAG 기억 검색
    ├── topic.py                 ← [소재] 소재 생성 / 오늘의 질문
    │
    ├── profile.py               ← [중재] 개인 말투 기준선
    ├── tone.py                  ← [중재] 룰 트리거 + 맥락 판정 + 문구 생성
    │
    └── prompts/
        ├── judge.md
        ├── topic.md
        ├── tone_judge.md
        └── tone_suggest.md
```

---

## 기능 명세 — 후보 기능 1. 대화 소재 제시

### 트리거 — 언제 개입하나

| | 패턴 | 판정 | 상태 |
| --- | --- | --- | --- |
| ① | 단답 핑퐁 (종료형 단답 3턴 연속) | 룰 | 구현 |
| ② | 질문 없는 대답 (되묻는 문장 0개로 3턴) | 룰 | 구현 |
| ③ | 한쪽만 발화 (비중 75% + 반대쪽 리액션만) | 룰 | 구현 |
| ④ | 일상 보고형 반복 | **LLM** | 구현 |
| ⑤ | 대화 중 정체 (20분 경과, 양쪽 접속 중) | 룰 | 구현 |

④는 룰로는 잡히지 않는다. 문장 길이·응답 속도·이모티콘이 전부 정상 범위인데도 매일 같은 패턴이 반복되는 상태다. 권태기의 가장 전형적인 모습이므로, 룰이 "정상"으로 판정해도 동일 패턴 반복이 감지되면 LLM에 넘긴다.

명세서의 ⑥ 장기 신호(최근 2주 vs 이전 4주 비교)는 **MVP에서 제외**한다. 6주치 데이터가 필요해 해커톤 시연에서 재현이 불가능하고, 목데이터로 만들어도 심사위원이 확인할 방법이 없다. 구현하지 않으며 관련 통계 수집 코드도 넣지 않는다.

**트리거는 "새 메시지 수신" 시점에만 평가한다.** 스크롤 연동과 재생 모드는 MVP에서 제외됐다.

### 노출 대상 (scope) 결정 — 룰

발화량 비율로 결정한다. LLM 호출 불필요.

| 조건 | scope | target |
| --- | --- | --- |
| 양쪽 다 조용 | `common` | — |
| 한쪽 발화 70% 이상 | `individual` | 말 거는 쪽 |
| 한쪽 단답 70% 이상 | `individual` | 단답 보내는 쪽 |

트리거 ①②③⑤는 감지 시점에 이미 발화 비율을 계산하므로 그 값을 재사용한다. 트리거 ④는 LLM을 태우므로 같은 호출에서 함께 판정한다.

### 소재 산출

**우선순위**
1. **기억 기반** — `memories`에 미소환(`used_at IS NULL`) 항목이 있으면 우선. LLM이 원문·시점을 포함해 자연스러운 문장으로 생성
2. **오늘의 질문** — 사전 풀 30개에서 선택. **LLM 호출 없음**, 즉시 응답

**적합도 검증 4종**
- 최근 30일 내 같은 소재를 던지지 않았는가
- 현재 시간대에 적합한가 (심야엔 무거운 질문 제외)
- 답하기 부담이 없는가 (감정·관계 질문은 후순위)
- 관계 상태를 언급하지 않는가 ← **절대 제약**

### 출력 형태

```python
class Decision(BaseModel):
    kind: Literal["topic", "tone"] = "topic"   # 후보 기능 종류
    scope: Literal["common", "individual"]
    target: Literal["A", "B"] | None   # individual일 때만
    content: str                        # 본문
    reason: str | None                  # 보조 문구
```

**`kind`가 프론트 렌더링 계약이다.** 후보 기능마다 화면 배치가 다르다.

| `kind` | 중앙 (크게) | 보조 (작게) |
| --- | --- | --- |
| `topic` | `content` — 대화 소재 | `reason` — 근거 문구, **하단** |
| `tone` | `content` — 대체 문장 | `reason` — 방향 문구, **상단** |

기본값이 `"topic"`이라 기존 계약은 깨지지 않는다.

화면 렌더링 구성 (프론트 담당):
- 상단: kakapo 안내 문구 (작게) — 공통은 "kakapo가 두 분께 질문 하나 가져왔어요", 개별은 "{이름}님, 이런 얘기 어때요?"
- 중앙: 소재 본문 (크게)
- 하단: 근거 문구 (작게)

서버는 `scope`와 `target`을 보고 **수신자별로 갈라서** 전송해야 한다. 방 단위 브로드캐스트만 하면 개별 코멘트가 양쪽에 다 뜬다.

### 예외 처리

- 사용 가능한 기억이 없으면 오늘의 질문으로 대체하고, **근거 문구는 생략**한다
- 바쁨을 알리는 표현("나 지금 회의", "이따 톡할게")이 있으면 트리거하지 않는다. 진짜 바쁜 것인지 핑계인지 구분은 LLM이 판단한다

---

## 기능 명세 — 후보 기능 2. 갈등 중재 (말투 교정 제안)

감정이 격양되어 공격적·오해 유발 표현을 보낸 사람에게 **그 사람에게만** 제시한다.
기능 성격은 갈등 중재, 개입 갈래는 관찰이다.

### 트리거 — 언제 개입하나

기존 트리거(①~⑤)가 "대화 흐름"을 보는 것과 달리 **방금 전송된 메시지 하나**를 본다.

| kind | 패턴 | 예시 |
| --- | --- | --- |
| `insult` | 인신공격 · 욕설 | |
| `generalization` | 일반화 화법 | "넌 늘 그런 식이다", "한 번을 안 챙겨주냐" |
| `sarcasm` | 비꼬는 어투 · 반어법 | "잘한다 ㅋㅋ", "대단하다 정말" |
| `address_change` | 호칭 변화 | 오빠 → 야, 너는 |
| `repetition` | 비슷한 말 반복 | "전화 받아" → "받아" → "받으라고" |
| `abrupt_change` | 평소와 다른 급변 | 마침표 종결, ㅋ 사라짐, 갑자기 짧아지거나 길어진 텍스트 |

`abrupt_change`는 **약한 증거다.** 단답 핑퐁이나 대화 정체 상황에서도 그대로 걸려서, 다른 신호와
함께일 때만 세고 혼자서는 하위 신호 3개 이상일 때만 인정한다. 이 제한이 없으면 후보 1의 케이스를
전부 가로챈다.

### 개인별 평소 말투 기준선 — 이 기능의 핵심

**같은 문장이라도 사람마다 뜻이 다르다.** 평소 "ㅇㅇ"를 자주 쓰고 그게 일상화된 커플에게
"ㅇㅇ"은 무례가 아니다. 원래 호칭이 "야"인 커플에게 "야"는 호칭 변화가 아니다.

**특정 단어를 기준으로 잡지 않고, 맥락과 함께 그 사람의 평소 대비 변화량으로 판정한다.**

| 측정 항목 | 예시 |
| --- | --- |
| 마침표 사용률 | 평소 3% → 이번 메시지 마침표 종결 |
| 사용 호칭 상위 2개 | '오빠', '자기' → '야' |
| 평균 메시지 길이 | |
| ㅋ 개수 | 평소 3개 → 0개 |
| 이모티콘 사용 빈도 | |
| 갈등 시 어휘 패턴 | "이 사람은 화나면 존댓말로 바뀐다" |

마지막 항목(`conflict_style`)은 계산으로 뽑을 수 없어 시드에만 있다. `data/speaker_profiles.json`.

직전 3턴도 함께 넘긴다. **반어·장난 판별용**이다 — 상대가 지금 무엇에 대해 말하고 있었는지에
따라 같은 표현의 의미가 달라진다.

### 판정 → 생성

```
[메시지 전송] → [룰 트리거] → [LLM 맥락 판정] → [방향 문구 + 대체 문장] → [보낸 사람에게만]
```

LLM 호출을 둘로 나눈 이유: 이 기능의 최대 리스크는 장난을 갈등으로 오인하는 것이다.
판정 프롬프트를 판정에만 집중시키고, 통과한 경우에만 생성한다. 대부분은 판정에서 걸러지므로
호출 비용도 오히려 줄어든다.

**방향 문구** — 왜 다르게 읽힐 수 있는지 한 줄. 둘 중 상황에 맞는 쪽을 쓴다.
- 감정을 알아봄: "지금 감정이 조금 올라와 있는 것 같아요"
- 표현을 짚음: "'맨날'은 지금 한 번이 아니라 그동안 전부로 들릴 수 있어요"

**대체 문장** — 지금 이어서 보낼 수 있는 문장 1개. **나 전달법**으로 쓴다.
주어를 '너'에서 '나'로 바꿔 행동과 감정만 남긴다.

| 요소 | 내용 |
| --- | --- |
| 상황(사실) | 상대의 행동·객관적 상황을 비난 없이 그대로 |
| 영향 | 그 행동이 나에게 준 구체적인 영향 |
| 감정 | 내가 느낀 솔직한 감정 |
| 바람 | 앞으로 이렇게 해주면 좋겠다는 구체적 요구 |

> "네가 약속 시간에 늦을 때(상황), 내가 기다리느라 힘들어(영향). 그래서 많이 속상했어(감정).
> 다음에는 미리 연락해 주면 좋겠어(바람)."

네 요소를 전부 넣으면 길어진다. 채팅으로 보낼 수 있는 길이가 우선이라 줄여도 된다.

### 제약 사항

**복사 버튼을 만들지 않는다.** 결국 자신이 직접 교정안을 보고 작성해야 의미가 있다.
따라서 대체 문장은 짧고 외우기 쉬워야 한다.

### 예외 — 이게 이 기능의 안전장치다

**맥락 이해가 가장 중요하다.** "와 미친 ㅋㅋ", "야 이 바보야 ㅋ" 처럼 비속 표현이 있어도
맥락상 욕설이 의도가 아닌 경우를 걸러내야 한다.

여기서 잔소리가 뜨면 사용자는 앱을 지운다. **애매하면 개입하지 않는다.**
`fixtures/case8_banter.json`이 이 케이스를 검증한다.

---

## 기억 저장소 + RAG

명세서의 "기억 기반 소재"를 위한 저장소. 없으면 항상 오늘의 질문만 나와서 LLM이 필요 없는 서비스가 된다.

**검색은 RAG로 한다.** 단순 최신순 조회가 아니라 현재 대화 맥락과 의미적으로 유사한 기억을 찾는다.

```
대화: "아 배고파... 뭐 먹지"

최신순 조회  → "작년에 갔던 방탈출 어땠어요?"      (맥락 무관)
유사도 검색  → "먹고 싶다던 그 파스타집 아직인가요?"  (맥락 연결)
```

이 차이가 "AI가 진짜 듣고 있다"는 느낌을 만든다. 시연에서 가장 잘 먹히는 지점이다.

### 구현

```python
from langchain_openai import OpenAIEmbeddings
from langchain_core.vectorstores import InMemoryVectorStore

store = InMemoryVectorStore(OpenAIEmbeddings(model="text-embedding-3-small"))
store.add_texts(contents, metadatas=metas)

hits = store.similarity_search(recent_context, k=3)
```

기억이 10~15건 규모라 `InMemoryVectorStore`로 충분하다. pgvector나 Chroma를 띄우지 않는다.
프로세스 시작 시 `data/memories.json`을 읽어 한 번 인덱싱한다.
후속 단계에서 같은 인터페이스로 pgvector에 갈아끼운다.

### 데이터 형태

```json
{
  "id": "m01",
  "kind": "place",
  "content": "성수동 카페 ○○",
  "source_quote": "여기 분위기 진짜 좋다 다음에 또 오자",
  "occurred_at": "2025-10-14T15:20:00+09:00",
  "used_at": null
}
```

`used_at`이 "미소환 우선"과 "30일 내 중복 금지"를 동시에 처리한다.
유사도 검색 결과 k개 중 `used_at`이 `null`인 것을 우선 선택한다.

### 기억 추출

별도 배치 파이프라인을 만들지 않는다. 개입 판정 LLM 호출에서 기억 추출을 함께 수행한다.

| kind | 의미 | 예시 |
| --- | --- | --- |
| `place` | 함께 간 장소 | 작년에 갔던 그 카페, 처음 만났던 동네 |
| `activity` | 함께 한 활동 | 같이 본 영화, 방탈출, 여행 |
| `promise` | 지키지 못한 약속 | "다음에 가자"고 했는데 안 간 곳 |
| `wish` | 저장만 하고 안 쓴 위시 | 먹고 싶다고 했던 것, 보냈던 링크 |
| `interest` | 두 사람의 관심사 | 좋아하는 연예인, 취미, 공통 화제 |

**시드 데이터가 필수다.** 실시간 추출만으로는 데모 시작 시점에 기억이 0건이라 오늘의 질문만 나온다. 커플의 3개월치 가상 대화에서 뽑은 기억을 미리 넣어둔다.

현재 시드는 **27건**이다 (place 6 / activity 6 / promise 5 / wish 5 / interest 5).
처음엔 12건이었는데, 대화 맥락이 뚜렷하지 않으면 아무 기억에나 붙어서 늘렸다.
`m02`만 `used_at`이 찍혀 있어 소환 메커니즘을 시연에서 보여준다.

---

## 후보 기능 라우터

명세서상 "개입 방향 결정"은 여러 후보 기능 중 1개를 골라 실행하는 구조다.
**위젯 슬롯이 1개**이기 때문이다.

**현재 구현된 후보는 2개다.**

| 순위 | 후보 | 이유 |
| --- | --- | --- |
| 1 | 갈등 중재 (`tone`) | 갈등이 감지된 순간에 대화 소재를 던질 때가 아니다 |
| 2 | 대화 소재 (`topic`) | |

`router.py`는 **우선순위 체인**이다. 앞 후보가 `None`을 돌려주면 다음으로 내려간다 —
말투 게이트가 걸렸지만 LLM이 "장난"으로 판정하면 대화 소재 후보로 넘어가는 식이다.

후보를 추가하려면 `build(ctx) -> Decision | None`을 구현해 `CANDIDATES`에 끼운다.
지금 단계에서 정교한 점수 기반 스코어링을 구현하지 않는다. 후보가 늘어 우선순위만으로
못 정하게 되면 그때 바꾼다.

나머지 후보 기능 명세는 PM이 작성 중이다.

---

## 시연 대비

해커톤 목표는 심사위원에게 "진짜 웃긴 팀, 잘되겠다"는 반응을 얻는 것이다.

- 3분 데모에서 보여줄 수 없는 기능에 시간을 쓰지 않는다
- 목데이터/시드 데이터가 필요한 것: 기억 27건, 개인 말투 기준선 2명, 시연용 대화 스크립트 8개
- 시연 시작 즉시 첫 개입이 나와야 한다. 기억 기반 소재("작년에 가셨던 그 카페")가 첫 출력으로 나오도록 시드를 구성한다
- **반복 시연은 `--no-persist`로 돌린다.** 안 그러면 소환된 기억에 `used_at`이 찍혀서 두 번째부터 다른 소재가 나온다

### 시연 추천 순서

| 케이스 | 보여주는 것 |
| --- | --- |
| `case3_one_sided` | RAG — 지하철 얘기 중에 예전 지하철 기억을 꺼낸다. "AI가 진짜 듣고 있다" |
| `case7_tone` | 갈등 중재 — 공격 표현 → 보낸 사람에게만 대체 문장 |
| `case8_banter` | 안전장치 — 똑같이 비속 표현인데 장난이면 아무것도 안 뜬다 |
| `case5_busy` | 안전장치 — 바쁘다고 하면 말 안 건다 |

`case7`과 `case8`을 붙여서 보여주는 게 가장 설득력 있다. 감지가 어휘 필터가 아니라
맥락 판정이라는 걸 한 번에 보여준다.

---

## 작업 원칙

- 명세와 코드가 어긋나면 **명세가 맞다.** 명세를 바꿔야 한다고 판단되면 코드를 임의로 고치지 말고 사용자에게 먼저 알린다
- 서버 연동 규격은 팀 합의 사항이다. 한쪽만 바꾸면 상대 서버가 깨진다
- 확실하지 않으면 추측해서 진행하지 말고 질문한다
